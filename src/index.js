import {
  accentColors,
  palettes,
  themes,
  themeSizes,
  sizes,
  calculateSize,
  calculateMargin,
} from './themes';

import { themeReducer } from './themes-reducer';
import { changeThemeSize, changeTheme, changeAccentColor } from './themes-actions';

export {
  accentColors,
  palettes,
  themes,
  themeSizes,
  sizes,
  calculateSize,
  calculateMargin,
  themeReducer,
  changeThemeSize,
  changeTheme,
  changeAccentColor,
};
